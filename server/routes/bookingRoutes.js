const express = require('express');
const router = express.Router();

// Placeholder for booking routes
// TODO: Implement booking controller and routes

router.get('/', (req, res) => {
  res.json({ message: 'Booking routes - Coming soon' });
});

module.exports = router;
